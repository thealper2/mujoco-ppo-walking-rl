# mujoco-ppo-walking-rl

Simple Tkinter dashboard for PPO training on MuJoCo tasks.

## Setup
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

## Run
python main.py

## Tests
pytest
